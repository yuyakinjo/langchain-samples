{
  "name": "langchain-samples",
  "module": "index.ts",
  "scripts": {
    "lint": "biome check --apply .",
    "format": "prettier --check './**/*' && biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  },
  "type": "module",
  "dependencies": {
    "langchain": "^0.1.21"
  }
}
